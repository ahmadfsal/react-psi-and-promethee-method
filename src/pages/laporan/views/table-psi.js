import React, { memo } from 'react'
import { Table } from 'libs'

const TablePsi = () => {
    return (
        <div className='table-container'>
            <Table></Table>
        </div>
    )
}

export default memo(TablePsi)
